From 0b514e66f97e1b8b88919d69e38f27e71198c1b8 Mon Sep 17 00:00:00 2001
From: Christian Stewart <christian@paral.in>
Date: Wed, 27 Apr 2022 15:58:27 -0700
Subject: [PATCH] Revert drm/meson: not load RGB709 to YUV709 coefficient

This reverts commit 0f000a3a1df5fbf0e6d5a72b2e6755a5ea97d87e.
---
 drivers/gpu/drm/meson/meson_viu.c | 3 ---
 1 file changed, 3 deletions(-)

diff --git a/drivers/gpu/drm/meson/meson_viu.c b/drivers/gpu/drm/meson/meson_viu.c
index dba2ca484b26..b09cb903c4a8 100644
--- a/drivers/gpu/drm/meson/meson_viu.c
+++ b/drivers/gpu/drm/meson/meson_viu.c
@@ -471,12 +471,9 @@ void meson_viu_init(struct meson_drm *priv)
 	if (meson_vpu_is_compatible(priv, VPU_COMPATIBLE_GXM) ||
 	    meson_vpu_is_compatible(priv, VPU_COMPATIBLE_GXL))
 		meson_viu_load_matrix(priv);
-#if 0
-	/* FIXME: */
 	else if (meson_vpu_is_compatible(priv, VPU_COMPATIBLE_G12A))
 		meson_viu_set_g12a_osd_matrix(priv, RGB709_to_YUV709l_coeff,
 					       true);
-#endif
 
 	/* Initialize OSD1 fifo control register */
 	reg = VIU_OSD_DDR_PRIORITY_URGENT |
-- 
2.35.1

