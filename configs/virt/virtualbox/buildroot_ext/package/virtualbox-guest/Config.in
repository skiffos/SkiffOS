config BR2_PACKAGE_VIRTUALBOX_GUEST
	bool "virtualbox-guest"
	depends on BR2_LINUX_KERNEL
	help
	  VirtualBox guest kernel modules.

	  https://www.virtualbox.org/

comment "virtualbox-guest needs a Linux kernel to be built"
	depends on !BR2_LINUX_KERNEL
