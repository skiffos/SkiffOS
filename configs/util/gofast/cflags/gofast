# safer without standards-breaking ffast-math
-fno-fast-math
# lto
-flto=auto -fuse-linker-plugin -fipa-pta 
# perform devirtualization across object file boundaries with LTO
-fdevirtualize-at-ltrans
# graphite (disabled on default)
# -fgraphite-identity -floop-nest-optimize
# assumes that interposition happens for functions,
# the overwriting function will have precisely the same semantics.
# (disabled on default)
# -fno-semantic-interposition
