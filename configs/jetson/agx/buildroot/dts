# NOTE: NVIDIA does not yet provide all the dts sources.
# Instead we will copy them from the linux4tegra binaries.
# BR2_LINUX_KERNEL_DTS_SUPPORT is not set
# BR2_LINUX_KERNEL_INTREE_DTS_NAME is not set

# Jetson AGX Xavier Devkit
# BR2_LINUX_KERNEL_DTS_SUPPORT=y
# BR2_LINUX_KERNEL_INTREE_DTS_NAME="nvidia/tegra194-p2972-0000"
